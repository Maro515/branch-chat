# Branch Chat（仮称）開発計画 v1

作成日: 2026-09-18
場所: `~/Applications/branch-chat/index.html`（単一HTML・オフライン動作・ビルド不要）
プレビュー: ルート `.claude/launch.json` の `branch-chat`（port 8991）

---

## 0. 目的

AIチャットを「直線タイムライン」ではなく「平面タイムライン」で行う。

- 本線 A の途中で **分岐モード** に入ると、そこから話題 B が始まる
- B から C へ再分岐してもよい
- 分岐元は「現在」でなくてよい。A の過去の AI 回答から分岐できる
- **俯瞰モード** でどこから何が分岐したかを一望し、クリックで A に何事もなかったように戻れる
- 最重要: **AI が A から派生した全ブランチを認知した状態で答える**
  （既存製品はどれも「祖先パスのみ」を AI に渡すため、兄弟ブランチを知らない）

## 1. 既存サービス調査（2026-09時点）

| 製品 | 分岐UI | 俯瞰 | 過去からの分岐 | AIが他ブランチを認知 |
|---|---|---|---|---|
| ChatGPT「Branch in new chat」 | 別チャットを開く | なし | ○ | × |
| Claude.ai（編集/再生成の隠れツリー） | 暗黙 | なし（Nodea拡張が可視化） | ○ | × |
| Nodea / KnowTree / Chatvas / canvas-chatbot / tldraw branching-chat | ノード分岐 | ○ キャンバス | ○ | × 「祖先パスのみ、兄弟は含めない」と明記 |
| Flowith（キャンバス型） | ノード接続 | ○ | ○ | △ ユーザーが手動でノードを線で繋いだ分だけ |

結論: 「分岐＋俯瞰」は既製品で実現済みだが、「全ブランチを AI が自動的に認知する」機能を持つ製品は見当たらない。ここが本アプリの差別化点であり、設計の中心。

## 2. コア設計: コンテキスト組み立て（Context Assembly）

AI へ送る各リクエストを 3 層で組み立てる。

```
[system]
  ├ 役割指示
  ├ 会話マップ（全ブランチのカード）   ← Layer 2
  └ 参照ブランチの全文（任意）        ← Layer 3
[messages]
  └ 現在ノードまでの祖先パス（全文）   ← Layer 1
```

### Layer 1: 祖先パス
現在ノードから root まで遡った user/assistant メッセージをそのまま messages に入れる。
既存製品と同じ部分。

### Layer 2: 会話マップ（ブランチカード）— 本アプリの核
全ブランチ（現在ブランチ以外も含む）について、以下のカードを system に注入する。

```
## 会話マップ
現在位置: ブランチ B（A の #12 「〜」から分岐）

- A（本線）: [要約 3〜5行] 未解決: … 結論: …
- B（現在）: [要約]
- C（B の #4 から分岐）: [要約] 結論: …
```

- 要約は各ブランチで AI の回答が確定するたびに **Haiku で非同期更新**（本文モデルとは別・安価）
- 要約の型を固定する: `title / 分岐元 / 何を話したか / 得られた結論 / 未解決の問い`
- 「A に戻ったとき、B や C で決めた事を踏まえて続きができる」のはこの層が担保する

### Layer 3: 参照ブランチの全文（ピン留め）
要約では足りない場合、ユーザーが俯瞰モードでブランチを「参照」に指定すると全文が system に入る。
（例: C の数式展開をそのまま踏まえて A で話したいとき）

### トークン予算
- 予算上限（例 60k）を設定し、超過時は Layer 1 の古いターンを rolling summary に置換
- 優先順位: 直近の祖先 > ブランチカード > ピン留め全文 > 古い祖先
- 各リクエストの推定トークン数を UI に表示

### 合流（merge）
ブランチの「結論」を本線に取り込む操作。
実装は「AI に『B の結論を A の文脈で 1 メッセージに要約して』と依頼し、A に assistant ノードとして追加（origin=B と記録）」。
分岐したまま放置された話題を回収する導線になる。

## 3. データモデル

```js
conversation = {
  id, title, createdAt,
  nodes: {
    [nodeId]: { id, parentId, role: 'user'|'assistant', content,
                branchId, ts, model, mergedFrom?: branchId }
  },
  branches: {
    [branchId]: { id, name, color, forkFromNodeId,   // null = 本線
                  headNodeId, summary: {...}, pinned: bool, status: 'open'|'closed' }
  },
  activeNodeId
}
```

- ノードはツリー（parentId のみ）。ブランチは「ツリーの中の名前付きパス」
- 分岐 = 任意ノードを forkFromNodeId とする新ブランチを作り、次の user ノードの parentId をそのノードにする
- 永続化: localStorage（会話ごとに 1 キー、5MB 超過時は IndexedDB へ移行）
- JSON エクスポート/インポート

## 4. UI: 3 モード

### Focus モード（通常チャット）
- 現在ブランチの祖先パスを直線表示（分岐元より前は本線の内容をそのまま表示）
- 各 AI 回答に「⑂ ここから分岐」ボタン
- 分岐点になっているメッセージにはバッジ「→ B, C が分岐」。クリックで飛べる
- 上部にパンくず: `A #12 → B #4 → C`
- 右上に常時ミニマップ（俯瞰の縮小版）

### 分岐モード
- 「⑂」押下 → ブランチ名入力（空なら最初の質問から自動命名）→ 色を自動割当
- そのまま入力欄にフォーカスし、B の最初の発言を打てる
- 過去ノードからの分岐も同じ操作（Focus モードでスクロールして過去回答の ⑂ を押す）

### 俯瞰モード（Overview）
- SVG で描く **地下鉄路線図/git graph 型**。縦=時間、横=ブランチのレーン
- ノード=丸、ブランチ=色線、分岐点=線の分かれ目。ホバーで冒頭 100 字
- クリックで Focus モードへ（そのノードを activeNodeId に）
- ブランチカード（要約）を横に並べて表示。「参照にピン留め」「合流」「閉じる」ボタン
- 無限キャンバス＋ドラッグは P2 で。まずは自動レイアウト（レーン割当）で十分

## 5. 技術方針

### 定額枠（Pro/Max）で使う方法 — 実装済み
API従量課金だけでなく、`bridge.py`（ローカルHTTPサーバ）が `claude -p`（Claude Code のヘッドレスモード）を呼ぶ経路を用意した。
Claude Code のログイン（サブスクリプション）で動くため APIキー不要。`rate_limit_event` が `five_hour` 窓を返すことを確認済み（＝定額枠で消費）。
`--tools "" --strict-mcp-config --no-session-persistence --system-prompt` で素の会話モデルとして使い、入力トークンは数百程度に抑えている。
制約: `claude -p` は単一プロンプトなので履歴を1本のテキストに畳んで渡す（API経路は messages 配列をそのまま使う）。

- 単一 HTML、外部依存なし、`python3 -m http.server` でプレビュー、ダブルクリックでも起動
- Claude API をブラウザから直接呼ぶ（`anthropic-dangerous-direct-browser-access: true`、Amadeus と同方式）
  - 本文モデル: 既定 `claude-sonnet-5`、選択肢 `claude-fable-5-1` / `claude-haiku-4-5-20251001`
  - 要約モデル: `claude-haiku-4-5-20251001` 固定
  - ストリーミング表示
- API キーは localStorage（個人ローカル利用前提。配布時は注意書き）
- キー未設定時は「ダミー応答モード」で UI だけ試せる（分岐・俯瞰の検証用）
- 将来: Ollama など OpenAI 互換ローカル API も選べるよう fetch 層を抽象化

## 6. プロンプト設計（骨子）

```
あなたは分岐型チャットのアシスタントです。この会話は 1 本の線ではなく、
複数のブランチ（話題）に分かれています。以下の「会話マップ」に全ブランチの
要約があります。ユーザーが別ブランチの内容に言及したら、そこで得られた結論を
踏まえて答えてください。ただし回答は現在ブランチの話題に集中してください。
現在ブランチ以外の内容を持ち出すときは「（B で決めた通り）」のように出典を示してください。
```

## 7. 開発フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| P0 MVP | ツリーモデル / Focus / ⑂分岐 / 俯瞰SVG / Claude API ストリーミング / Layer1+2（要約は Haiku で毎ターン更新） / localStorage | 下記シナリオテスト S1〜S3 が通る |
| P1 | トークン予算と rolling summary / ピン留め（Layer3） / 合流 / トークン数表示 / ダミー応答モード | 長い会話（100 ターン超）でも予算内に収まる |
| P2 | 複数会話管理 / 検索 / Markdown 描画・コピー / JSON 入出力 / 俯瞰のドラッグ・ズーム | 実運用 1 週間で不便がない |
| P3 | iPhone レイアウト / PWA / Ollama 対応 / ブランチ自動命名の改良 | 任意 |

## 8. 検証シナリオ

- S1: A で薬剤 X の話 → #6 から B（副作用）に分岐 → B で結論 → A に戻って「B で分かった副作用を踏まえて投与計画を」と聞く → AI が B の結論を引用して答える
- S2: B の途中から C に分岐 → C の内容を A 側で聞いても答えられる（孫ブランチの認知）
- S3: A の過去回答（#3）から D を分岐 → D の messages が #3 までで終わり、#4 以降を含まないこと（祖先パスの正しさ）
- S4: 同じ質問を「分岐なしの直線チャット」と比較し、無関係なブランチが本線の回答を汚染しないこと（system の「現在ブランチに集中」指示の効果）

## 9. リスクと対策

- **要約の取りこぼし**: 要約に「結論/未解決」の型を強制。重要箇所はピン留め全文で補う
- **コスト**: 毎ターン Haiku 要約が走る。差分要約（新ターンのみ渡す）で抑える
- **ブランチ数の爆発**: 俯瞰でブランチを「閉じる」と要約 1 行に縮む
- **API キーのブラウザ保存**: 個人利用限定。共有 PC では「セッション終了時に消す」オプション
- **CORS**: Claude API は上記ヘッダで可。他プロバイダは非対応の可能性あり

---

# デスクトップ版 計画（2026-09-19 開始）

場所: `desktop/`（Electron）。Artifact版で使用感を固めた後の本命。利用者は自分の Claude Code / Codex CLI のログイン＝定額枠で使う。

## 方針

- **画面は `index.html` をそのまま使う。** `desktop/app/` へは `npm run sync` がコピーするだけ（手で編集しない・git管理しない）。UIの正は常に親フォルダの `index.html`。
- **`bridge.py` の役割を Electron 本体（Node）に内蔵**する（`desktop/engines.js`）。利用者に Python もターミナル操作も要求しない。
- **独自スキーム `app://branchat/`** で配信し、`/api/status` と `/api/chat`（SSE）を本体が処理する。画面側のコードは `fetch('/api/chat')` のまま動く。オリジンが固定なので localStorage の会話が起動ごとに消えない。ローカルHTTPサーバーもポートも使わない。
- 安全側の設定は `bridge.py` と揃える（Claude: ツール無し・MCP無し・履歴無し / Codex: 読み取り専用・履歴無し・利用者設定を読まない）。レンダラは `contextIsolation`＋`sandbox`、Node 連携なし、外部リンクは既定ブラウザへ。

## フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| D0 骨組み | Electron 起動、app:// 配信、Claude/Codex エンジン、`npm run smoke`、`npm run pack:dir` | スモークテスト合格（画面表示・エンジン検出・ダミー送信・実モデル1回）、.app が起動する |
| D1 配布品質 | アプリアイコン、フォント同梱（オフライン対応）、会話の自動バックアップ（userData に JSON）、CLI 未検出時のセットアップ案内、生成の中断ボタン | ネット無しでも表示が崩れない、アプリを消しても会話を復元できる |
| D2 MCP | 設定で使う MCP サーバーを選択（`--mcp-config`）、読み取り系ツールのみ自動許可、書き込み系は確認、ツール実行を回答の上に表示 | PubMed 検索を使った回答が分岐の文脈に残る |
| D3 配布 | Windows ビルド、署名・公証、配布方法、更新の仕組み | 他の利用者のPCで起動し、各自のログインで動く |

## 既知の注意

- GUIアプリは PATH が最小限。`engines.js` がログインシェルの PATH と定番の場所（`~/.local/bin`、Homebrew、ChatGPT.app 同梱の codex）を探す。
- Codex の回答はまとまり単位で届く（1文字ずつにならない）。
- ブラウザ版・Artifact版とは保存先が別。会話の移動は「書き出し / 読み込み」。
